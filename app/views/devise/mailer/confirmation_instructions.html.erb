<p><%= I18n.t('confirmations.title', recipient: @email) %></p>

<p><%= I18n.t('confirmations.instruction') %></p>

<p><%= "#{ENV["CONFIRM_URL"]}?confirmation_token=#{@token}" %></p>

<p><%= link_to I18n.t('confirmations.link_to_confirm'), "#{ENV["CONFIRM_URL"]}?confirmation_token=#{@token}" %></p>